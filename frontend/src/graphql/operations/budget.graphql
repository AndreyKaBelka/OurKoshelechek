query Budget($groupId: UUID!, $period: String!) {
  budget(groupId: $groupId, period: $period) {
    income {
      amount
    }
    totalAllocated {
      amount
    }
    free {
      amount
    }
  }
}
